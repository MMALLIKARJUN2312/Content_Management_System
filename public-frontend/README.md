# RenewCred Public Site

Next.js public frontend, consuming content from the backend API. See the
[root README](../README.md) for full setup, architecture, and environment variable docs.

```bash
npm install
npm run dev   # http://localhost:3000
```
